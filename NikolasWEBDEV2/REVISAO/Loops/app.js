// Initiation ; Condition ; Increment

for( let i = 0; i <= 10; i++)
    {
        console.log( i );
    }

//Creating a Multiplication Table
let numnum = 5;
let mult = 1;

for( mult ; mult <= 10; mult++)
    {
        console.log( `${numnum} x ${mult} = ` + numnum * mult);
    }






    // While

    let filmes = ["Sonic 3", "Iron Lung", "Superman 2025"];
    let Q = 0;

    for( Q ; Q < filmes.length ; Q++)
        {
            console.log( filmes[ Q ]);
        }